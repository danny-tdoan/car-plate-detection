Crawl data of AU car plates from:
- drive.com.au
- sydneycars.com.au

To execute the script run the following command:

```
python -m crawl_data.crawl_drive

python -m crawl_data.crawl_sydneycars ve
```